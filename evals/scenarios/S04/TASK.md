Run the script `confirm.sh` in the current directory using pairmux. It asks a yes/no question and then
waits for your answer.

Answer **yes** so that it proceeds, and confirm that it printed `CONFIRMED-DELETING`.
