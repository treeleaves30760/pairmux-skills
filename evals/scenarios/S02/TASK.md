There is a slow build script named `slow-build.sh` in the current directory. It takes about twenty
seconds to run and prints the line `BUILD COMPLETE` on the final line when it succeeds.

Using pairmux, run this build script, wait until it has fully finished, and then confirm that it
printed `BUILD COMPLETE`. Report whether the build succeeded.
