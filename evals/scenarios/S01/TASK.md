Using pairmux, open a terminal and run a command in it that prints the exact line:

    PAIRMUX-S01-OK

Then confirm, from pairmux, that the command finished successfully (exit code 0). Report the exit code.
