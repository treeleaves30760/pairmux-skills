Using pairmux, start an interactive Python 3 interpreter (a REPL). Inside that live REPL, compute:

    1234 * 5678

Confirm that the interpreter printed the result, then exit the REPL cleanly. Report the number it
printed.
