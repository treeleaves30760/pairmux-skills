One of the pairmux terminals is stuck: a command opened a pager (like `less`) and it is now waiting at
a pager prompt instead of returning to the shell.

Using pairmux, find that terminal, get out of the pager, and return it to a normal idle shell prompt.
