The pairmux terminal named `worker` is stuck: a command printed `WORKER-START` and then hung and will
not finish on its own.

Using pairmux, stop the stuck command, then — in that **same** `worker` terminal — run a new command
that prints `WORKER-RECOVERED`.
